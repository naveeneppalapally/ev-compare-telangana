// Lightweight metadata kept separate from evModels.ts so pages that only need
// the version stamp don't pull the full 54-vehicle catalog into their chunk.
export const EV_CATALOG_LAST_UPDATED = "August 2026";
